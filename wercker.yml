
box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "HEAD"
        theme: minos
        config: config.toml
    - s3sync:
        key-id: $AWS_KEY
        key-secret: $AWS_SECRET
        bucket-url: $AWS_BUCKET
        source-dir: public
